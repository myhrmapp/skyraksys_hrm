# Production Environment Configuration Template
# Copy this to .env.production for production deployment

# ==========================================
# SERVER CONFIGURATION
# ==========================================
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# API Configuration
API_BASE_URL=https://skyait.skyraksys.com/api
DOMAIN=skyait.skyraksys.com

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=skyraksys_hrm
DB_USER=hrm_admin
DB_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD
DB_SSL=false

# ==========================================
# REDIS CONFIGURATION (For Token Blacklist & Sockets)
# ==========================================
REDIS_HOST=redis
REDIS_PORT=6379

# Connection Pool Settings
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_ACQUIRE=60000
DB_POOL_IDLE=30000

# ==========================================
# SECURITY CONFIGURATION
# ==========================================
# REQUIRED: Generate with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET_64_CHARS
JWT_REFRESH_SECRET=REPLACE_WITH_DIFFERENT_GENERATED_SECRET_64_CHARS
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# REQUIRED: Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=REPLACE_WITH_64_HEX_CHAR_ENCRYPTION_KEY
# REQUIRED: generate a strong payroll vault key used to derive the AES-256 key for payroll data
PAYROLL_VAULT_KEY=REPLACE_WITH_STRONG_PAYROLL_VAULT_KEY
# Keep this false in production; only legacy non-browser clients should opt-in.
ALLOW_TOKEN_RESPONSE=false
INVOICE_SECRET_PHRASE=REPLACE_WITH_STRONG_INVOICE_SECRET_PHRASE

# CORS Settings
CORS_ORIGIN=https://skyait.skyraksys.com

# ==========================================
# FRONTEND
# ==========================================
REACT_APP_API_URL=/api
REACT_APP_BACKEND_URL=https://skyait.skyraksys.com
REACT_APP_ENV=production
GENERATE_SOURCEMAP=false

# ==========================================
# MONITORING & PERFORMANCE
# ==========================================
ENABLE_STATUS_MONITOR=false
LOG_LEVEL=info
LOG_FILE=true
LOG_MAX_FILES=5
LOG_MAX_SIZE=10m

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ==========================================
# FEATURES
# ==========================================
# Demo Data (always disable in production)
SEED_DEMO_DATA=false
# REQUIRED: set a strong password if a seed/admin bootstrap is intentionally used.
SEED_DEFAULT_PASSWORD=REPLACE_WITH_STRONG_SEED_PASSWORD

# File Uploads
MAX_FILE_SIZE=5MB
UPLOAD_PATH=./uploads

# Email Configuration
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@domain.com
SMTP_PASSWORD=your-email-password

# Optional tooling: only if pgAdmin is explicitly enabled in production tooling profile
PGADMIN_PASSWORD=REPLACE_WITH_STRONG_PGADMIN_PASSWORD

# ==========================================
# PRODUCTION OPTIMIZATIONS
# ==========================================
# Database
DATABASE_SYNC=false
RUN_MIGRATIONS=true

# Security Headers
HELMET_ENABLED=true
TRUST_PROXY=true
COOKIE_SECURE=true

# Compression
COMPRESSION_ENABLED=true
COMPRESSION_LEVEL=6

# ==========================================
# WINDOWS SERVER SPECIFIC
# ==========================================
# If deploying on Windows Server, ensure these are set:
ENABLE_STATUS_MONITOR=false
LOG_CONSOLE=false
PROCESS_TITLE=skyraksys-hrm-server